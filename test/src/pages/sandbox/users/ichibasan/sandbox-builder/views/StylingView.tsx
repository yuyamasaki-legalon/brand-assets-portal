import type React from "react";
import type { ContentArea } from "../types";

interface Props {
  area: ContentArea;
}

export const StylingView = (_props: Props): React.ReactElement | null => {
  return null;
};
