var e='# Task 1 — Project Setup: Install culori and Create Directory Skeleton\n\n## Objective\nInstall the `culori` color-math library, create the full palette-lab directory tree with stub files so TypeScript module resolution succeeds, and copy the Aegis seed palette to `assets/palette-initial.json`.\n\n## Input (what exists before this task)\n- `src/pages/sandbox/users/ichibasan/palette-lab/index.tsx` (existing, do not delete)\n- `src/pages/sandbox/users/ichibasan/sandbox-builder/token-overrides/palette.json` (read-only reference for copy)\n- `package.json` at repo root (for pnpm)\n\n## Output (what to create / modify)\n- `assets/palette-initial.json` — verbatim copy of `sandbox-builder/token-overrides/palette.json`\n- `color/oklch.ts` — stub (`export {}`)\n- `store/types.ts` — stub (`export {}`)\n- `store/actions.ts` — stub (`export {}`)\n- `store/reducer.ts` — stub (`export {}`)\n- `store/context.tsx` — stub (`export {}`)\n- `store/storage.ts` — stub (`export {}`)\n- `seed/seed.ts` — stub (`export {}`)\n- `utils/export.ts` — stub (`export {}`)\n- `components/FamilyList/index.tsx` — stub (`export {}`)\n- `components/SwatchRow/index.tsx` — stub (`export {}`)\n- `components/ToneEditor/index.tsx` — stub (`export {}`)\n- `components/ExportButton/index.tsx` — stub (`export {}`)\n- `components/ProjectSelector/index.tsx` — stub (`export {}`)\n\n## Acceptance Criteria\n- [ ] `culori` appears in `package.json` dependencies after `pnpm add culori`\n- [ ] `culori` can be imported without TypeScript errors (`import * as culori from "culori"` in a `.ts` file compiles clean)\n- [ ] All stub files listed above exist and contain at minimum `export {}`\n- [ ] `assets/palette-initial.json` content is byte-for-byte identical to the source `sandbox-builder/token-overrides/palette.json`\n- [ ] Running `pnpm build` (or `tsc --noEmit`) produces no new errors related to palette-lab module resolution\n\n## Implementation Notes\n\n### Installing culori\n\nRun in the repo root:\n\n```sh\npnpm add culori\n```\n\n**Type definitions**: `culori` v1+ ships its own TypeScript types; `@types/culori` is a separate legacy package from the DefinitelyTyped era. **Check first**: run `pnpm add culori` and then try `import type { Oklch } from "culori"` in a `.ts` file. If TypeScript resolves the type without error, you do NOT need `@types/culori`. If you get "Cannot find module \'culori\' or its corresponding type declarations", then run `pnpm add -D @types/culori`.\n\n### Copying the palette seed\n\nCopy the file contents manually (do not use a build script or symlink). The file at `assets/palette-initial.json` must be a static JSON asset that Vite can import with `import paletteInitial from "../assets/palette-initial.json"`. The path used in import statements will be relative to the importing file.\n\n### Stub file format\n\nEach stub file must be valid TypeScript that exports nothing but does not produce "module has no exports" errors. The minimal valid stub is:\n\n```typescript\nexport {};\n```\n\nDo NOT use `export default {}` — an empty default export confuses TypeScript when the real implementation later exports named values.\n\n### Directory layout\n\nAll paths are relative to `src/pages/sandbox/users/ichibasan/palette-lab/`:\n\n```\nassets/\ncolor/\nstore/\nseed/\nutils/\ncomponents/FamilyList/\ncomponents/SwatchRow/\ncomponents/ToneEditor/\ncomponents/ExportButton/\ncomponents/ProjectSelector/\n```\n\nCreate directories as needed. The `docs/` and `tasks/` directories already exist.\n';export{e as default};