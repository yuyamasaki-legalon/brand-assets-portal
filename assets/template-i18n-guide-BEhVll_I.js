var e='# Template / Sandbox i18n ガイド（統一規約）\n\ntemplate と sandbox の両方で使う、単一の多言語化（i18n）規約。SSOT は **loc-app のテキスト**。\n\n## 全体像\n\n```\nlib/loc-app/text-management/{service}/translate.csv   ← SSOT（テキストの真実。loc-app 側で編集）\n        │  値をコピー（出典コメントで紐づけ）\n        ▼\nsrc/pages/.../data/translations.ts                    ← useTranslation 用の辞書（コミットする）\n        │  pnpm i18n:drift で SSOT との差分を検証・取込\n        ▼\nindex.tsx 等                                          ← const { t } = useTranslation(...) で t("key")\n```\n\n- **i18n エンジン**: `src/hooks/useTranslation.ts`（`TranslationDictionary<K>` + `t(key)`）。`App.tsx` の\n  `LocaleUrlProvider` で全ページに配線済み。ロケールは URL `?lang=`（既定 `ja-JP`）と右下フローティング設定\n  （Tools → Provider locale）で切替。template 内に独自トグルは置かない。\n- **配置**: ページごとに `{page}/data/translations.ts`、共有レイアウトは `_shared/data/translations.ts`。\n- **キー**: **セマンティック**（`caseName`, `caseStatus`, `nav.home` 等）。可読で変数命名にも使える。\n  loc-app の messageKey をそのままキーにはしない（汎用キーの衝突・不透明さを避けるため）。\n\n## translations.ts の書き方\n\n```ts\nimport type { TranslationDictionary } from "../../../../../hooks"; // 深さはページ階層に合わせる\n\nexport type TranslationKey = "pageTitle" | "createCase" /* ... */;\n\nexport const translations: TranslationDictionary<TranslationKey> = {\n  "en-US": {\n    // legal-management-f: headerTitle @ /parts/index/ProUserMainContent/Header\n    pageTitle: "Matters",\n    // legal-management-f: createCaseDialogButtonLabel\n    createCase: "Create matter",\n    // mock-only (loc-app に対応の無いプロトタイプ文言)\n    emptyTitle: "No matters yet",\n  },\n  "ja-JP": {\n    // legal-management-f: headerTitle @ /parts/index/ProUserMainContent/Header\n    pageTitle: "案件",\n    // legal-management-f: createCaseDialogButtonLabel\n    createCase: "案件を作成",\n    // mock-only\n    emptyTitle: "案件がまだありません",\n  },\n};\n```\n\n利用側:\n\n```tsx\nimport { useTranslation } from "../../../../hooks";\nimport { type TranslationKey, translations } from "./data/translations";\n\nconst { t } = useTranslation<TranslationKey>(translations);\n// <ContentHeaderTitle>{t("pageTitle")}</ContentHeaderTitle>\n```\n\nモジュールトップレベルの配列（カラム定義・Select options 等）は `t` を使えないため、`t` を引数に取る\nfactory（例 `buildCaseColumns(t)`）にするか、コンポーネント内で構築する。\n\n## 出典コメント（SSOT 紐づけ・機械可読）\n\n各キーの**直前行**に 1 つ付ける。`pnpm i18n:drift` がこれを辿って loc-app の値と突き合わせる。\n\n| 形式 | 意味 | drift 検査 |\n|------|------|-----------|\n| `// {service}: {messageKey}` | loc-app translate.csv 由来（messageKey で照合） | する |\n| `// {service}: {messageKey} @ {directory}` | 同上だが `(directory, messageKey)` で厳密照合 | する |\n| `// lib/loc-app: {const path}` | proto / 定数由来（CSV 非掲載） | しない |\n| `// mock-only` | loc-app に対応の無いプロトタイプ専用文言 | しない |\n\n- `service` は `lib/loc-app/text-management/{service}/` のディレクトリ名（例 `legal-management-f`,\n  `loc-common-frontend`）。\n- **`@ {directory}` を付けるケース**: `headerTitle` のような汎用 messageKey は複数 directory に異なる値で\n  存在し曖昧になる。その場合は `translate.csv` の `directoryName` を `@` で添えて厳密照合する。\n- sandbox の独自文字列は出典コメント省略 or `// mock-only`。\n\n## 補間\n\n値に `{name}` 等のプレースホルダを残し、利用側で `replace` する（loc-app の `{{x}}` 形式はコピー時にこの形へ寄せる）。\n\n```ts\n// translations.ts:  selectedCount: "{count} selected" / "{count}件選択中"\nt("selectedCount").replace("{count}", String(n));\n```\n\n## drift 検査 / 取込（pnpm i18n:drift）\n\n```bash\npnpm i18n:drift                                            # 既定 glob（src/pages/**/data/translations.ts）\npnpm i18n:drift --fix                                      # SSOT(translate.csv) の値を辞書へ取込\npnpm i18n:drift "src/pages/template/loc/**/data/translations.ts"\n```\n\n- 出典コメントの `{service}:{messageKey}` を `translate.csv` で引き、`ja`/`en` の値ずれ・キー欠落・\n  （directory ヒント無しの）曖昧を報告する。\n- `--fix` はキー・構造・コメントを保持し、**値だけ** SSOT で更新する。`key-missing` / `ambiguous` は\n  自動修正せず手動対応（`@ {directory}` 追加やキー差し替え）を促す。\n- 実装: `scripts/loc-i18n-drift.ts` + 純粋ヘルパー `scripts/loc-i18n-drift.helpers.ts`（ユニットテスト付き）。\n\n> **用語集（`pnpm lint:translation`）との関係**: SSOT 由来（`// {service}:` 出典）の値は **loc-app の\n> 実際の英語が優先**。用語集（terminology.json）が SSOT 値に warning を出しても、それは SSOT 値を変えず\n> 用語集側で扱う。SSOT 由来エントリを用語集 `--fix` で書き換えない（drift の原因になる）。`lint:translation`\n> の既定 glob は sandbox のみで、SSOT 由来の template は対象外。\n\n## 共有サイドバー（_shared）\n\n`src/pages/template/loc/_shared` はナビゲーション SSOT を `loc-common-frontend`（GlobalSidebar）に持つ。\n\n- `_shared/data/translations.ts`: `nav.*` キー（GlobalSidebar の messageKey に出典）。\n- `locNavigation.ts`: トップレベル項目は `label` ではなく `labelKey: NavigationTranslationKey`。\n- `LocSidebarLayout.tsx`: `useTranslation(navigationTranslations)` で `{t(labelKey)}` を描画。\n- ja-JP が既定なので、i18n 化しても既存ページの見た目は不変（回帰なし）。\n\n### 既知の段階的範囲\n- サイドバーの **submenu（menuItems）** は現状未対応（ja のまま）。`t()` フォールバックで安全に表示される。\n  対応する場合は menuItems にも `labelKey` を導入し `nav.*` を拡張する。\n\n## loc-sync との関係\n\ntemplate は loc-sync スキルで loc-app から再生成される。i18n を恒久化するため、sync は\n`messages.en-US.csv`（= translate.csv の en 列）も読み、`useTranslation` ベースの `index.tsx` と\n`data/translations.ts` を出力する。テキスト変更（patch）は `pnpm i18n:drift --fix` で取り込む。詳細は\n`skills/loc-sync/SKILL.md` の「i18n（統一規約）」節を参照。\n';export{e as default};