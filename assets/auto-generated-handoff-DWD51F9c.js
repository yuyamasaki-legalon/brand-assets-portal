var e='# Auto-Generated Handoff - Palette Lab\n\n> **Note:** This file was auto-generated by AI. / このファイルは AI によって自動生成されました。\n\n<!-- GENERATED_AT: 2026-05-18T00:00:00+09:00 -->\n<!-- SANDBOX_PAGE: src/pages/sandbox/users/ichibasan/palette-lab/index.tsx -->\n\n## 目的\n\nAegis デザインシステムの OKLCH カラーパレットを設計・検証・エクスポートするための sandbox ツール。\n本番の `palette.json` / `palette.tokens.js` と互換性のある出力を生成することを最終目的とする。\n\n---\n\n## ディレクトリ構成\n\n```\npalette-lab/\n├── index.tsx                   # ページルート・レイアウト・グローバル状態\n├── assets/\n│   └── palette-initial.json    # Aegis 現行パレットのシードデータ\n├── store/\n│   ├── types.ts                # データモデル定義・定数・ヘルパー\n│   ├── actions.ts              # アクション型定義 (19 種)\n│   ├── reducer.ts              # 状態更新ロジック\n│   ├── context.tsx             # React Context + Provider\n│   └── storage.ts              # localStorage 永続化\n├── color/\n│   ├── oklch.ts                # culori ラッパー (OKLCH ↔ HEX 変換・gamut チェック)\n│   ├── neutral.ts              # Neutral ファミリー生成ロジック\n│   ├── primary.ts              # Primary 透過スケール生成\n│   └── contrast/\n│       ├── specs.ts            # コントラストチェック仕様定義 (~850 行)\n│       ├── resolve.ts          # トークン参照の解決ロジック\n│       └── index.ts            # barrel export\n├── seed/\n│   └── seed.ts                 # JSON → PaletteProject 変換\n├── utils/\n│   ├── export.ts               # palette.json 生成\n│   └── exportTokens.ts         # palette.tokens.js 生成 (DTCG 形式)\n└── components/\n    ├── ContrastCheckPanel/     # コントラスト検証パネル\n    ├── ExportButton/           # エクスポートトリガーボタン\n    ├── ExportDialog/           # エクスポートダイアログ (Base / Primary タブ)\n    ├── FamilyAxisPanel/        # ファミリー軸編集 (L/C/H カーブ)\n    ├── FamilyList/             # カラーファミリー一覧サイドバー\n    ├── PrimaryTab/             # Primary スケール確認タブ\n    ├── ProjectSelector/        # プロジェクト切り替え\n    ├── SwatchRow/              # スウォッチ行\n    ├── ToneAxisPanel/          # トーン軸編集 (全ファミリー一括)\n    ├── ToneCurve/              # L/C/H カーブグラフ (SVG)\n    └── ToneEditor/             # 個別トーン値エディタ\n```\n\n---\n\n## データモデル\n\n```typescript\n// store/types.ts\n\ninterface ToneEntry {\n  value: number;      // トーン番号 (50, 100, …, 900, 450=alt500)\n  lightness: number;  // OKLCH L (0–100)\n  chroma: number;     // OKLCH C (0–0.4)\n  hue: number;        // OKLCH H (0–360)\n  alphaMode: "none" | "transparent" | "primary";\n  hex: string;        // 派生値。reducer が L/C/H から自動計算\n}\n\ninterface ColorFamily {\n  id: string;\n  name: string;\n  tones: ToneEntry[];         // value 昇順でソート済み\n  primaryBaseTone: number | null;  // Primary スケールの基準トーン値\n  isBuiltIn?: boolean;        // true = Neutral (削除不可)\n}\n\ninterface PaletteProject {\n  id: string;\n  name: string;\n  createdAt: string;  // ISO 8601\n  updatedAt: string;\n  colorFamilies: ColorFamily[];\n}\n\ninterface PaletteLabState {\n  projects: PaletteProject[];\n  activeProjectId: string | null;\n  activeFamilyId: string | null;\n  activeToneValue: number | null;\n}\n```\n\n---\n\n## PageLayout / Sidebar 構成\n\n`index.tsx` の `PaletteLabContent` は `SidebarProvider` を controlled state で使い、`contrastOpen` / `setContrastOpen` を `open` / `onOpenChange` に渡す。メイン UI は `SidebarInset` に入れ、右側のコントラストチェックだけを Aegis `Sidebar` に分離している。\n\n主なレイアウト:\n\n- `SidebarProvider open={contrastOpen} onOpenChange={setContrastOpen}`\n- `SidebarInset` 内: `Header` + `PageLayout`\n- start pane: `PageLayoutPane position="start" open width="xLarge"`。`FamilyList` と `Add family` footer を保持する。`FamilyList` の先頭には App BG Lightness `TextField type="number"` を置き、その下に Background-Color `Combobox` を置く。2 項目は Aegis `Form size="small"` 内の `FormControl orientation="horizontal"` として構成する\n- content: `PageLayoutContent`。ファミリー軸 / トーン軸のカーブ編集 UI を保持する\n- contrast: `Sidebar side="inline-end" behavior="push" collapsible="offcanvas" resizable width="medium"`。`SidebarHeader` と `SidebarBody` に `ContrastCheckPanel` を配置する。初期幅は `medium` で、ユーザーが横幅をリサイズできる。Page BG の切り替え時は `--aegis-sidebar-background-color` を更新し、Sidebar 本体の `background-color` を `--aegis-motion-duration-normal` + `--aegis-motion-easing-default` で transition する\n\nHeader の `Contrast` ボタンは `setContrastOpen((v) => !v)` で controlled state を直接切り替え、`contrastOpen` に応じて `Button variant="solid" | "subtle"` を切り替える。Sidebar header の trailing には `SidebarTrigger` を置き、サイドバー内から閉じられるようにしている。\n\n### alt500 sentinel パターン\n\n```typescript\nexport const ALT_TONE_SENTINEL = 450;\nexport const toneLabel = (value: number): string =>\n  value === ALT_TONE_SENTINEL ? "alt500" : String(value);\n```\n\n- `ToneEntry.value` の型は `number` のまま変えない\n- JSON インポート時: `"alt500"` キー → `450`\n- JSON エクスポート時: `450` → `"alt500"` キー (`toneLabel` 経由)\n- 表示・aria-label・入力パース すべて `toneLabel` / `parseToneInput` を通す\n- alt500 は Primary スケール生成 (`buildPrimaryScale`) から除外される\n\n---\n\n## SwatchRow 選択スタイル\n\n`components/FamilyList/index.tsx` は Base タブで `activeFamilyId` と `activeToneValue` を `SwatchRow` に渡す。`components/SwatchRow/index.tsx` は同一 tone 列の選択状態を全ファミリー行に表示し、アクティブファミリーだけ黒い背面フレームで強調する。\n\n主なスタイル条件:\n\n- 選択 swatch は `borderRadius: var(--aegis-radius-medium)`（4px 相当）を使う。選択 ring は swatch 要素本体の outline ではなく、親 strip の白 / 黒背景を swatch 色面 `::before` の内側 inset で見せることで表現する。選択 swatch 自体の flex サイズは変えない。\n- 選択 swatch には `.swatchSelected` を付与し、`.swatchSelected::before { inset-block: 1px; inset-inline: 4px; }` で色面だけを視覚的に縮小する。900 / 50 以外では左右両側を 4px 縮める。900 選択時は `.swatchSelectedStart::before` で `inset-inline-start: 0` / `inset-inline-end: 4px` にし、50 選択時は `.swatchSelectedEnd::before` で `inset-inline-start: 4px` / `inset-inline-end: 0` にする。未選択に戻ると `.swatch::before` の `inset: 0` に戻る。swatch 要素本体には `transform: scale(...)` を使わない。transition は `--aegis-motion-duration-fast` と `--aegis-motion-easing-default` を使い、swatch 側の delay は `0s` のままにする。\n- アクティブファミリーに選択 tone がある場合、親 strip 自体の `background` は透明のままにし、`index.module.css` の `.stripActive::before` を黒い swatchbar 外周として使う。`::before` は `z-index: 0` / `pointer-events: none` / `inset: -2px` で背面に置き、children は wrapper 側の `zIndex: 1` で前面に置く。これにより、黒外周を出しても flex item の実寸は変わらない。\n- `.strip::before` は常に存在し、inactive 時は `background-color: #fff` / `inset: 0`、active 時は `background-color: #000` / `inset: -2px`。`inset` / `background-color` / `border-radius` を `--aegis-motion-duration-normal` + `--aegis-motion-easing-default` で transition するため、背景が白から黒へ変化しながら外側へ拡大して消失時は戻る。swatch が先、swatchbar 背景が少しあとに動くよう、`::before` だけ `--swatch-row-bg-delay: calc(var(--aegis-motion-duration-fast) / 4)` を transition delay に使う。\n- アクティブ row の 900 / 50 選択時は端だけ線が太くならないよう、外側の selection border は描かず、拡張した swatchbar `::before` を外側の線として使う。選択 swatch 側も border / padding ではなく、色面 `::before` の片側 inline inset で親 strip 背景を露出させる。\n- 900 / 50 の外端は未選択 swatch でも常に `var(--aegis-radius-medium)`（4px 相当）で丸める。アクティブ選択 row の親 strip は swatchbar 背景用に `6px` radius を使うが、active row の 900 / 50 swatch と選択 swatch に接する隣接 swatch の radius はすべて `var(--aegis-radius-medium)` に揃える。`::before` は `inset: -2px` で外側に広がるが、radius は `calc(... + 2px)` にせず `var(--swatch-row-radius, 6px)` のままにする。\n- 親 strip は `overflow: visible` にする。選択時の黒外周は `::before` が外側へ 2px はみ出すため、親で clip しない。\n- `prefers-reduced-motion: reduce` では `.strip::before` と `.swatch` の transition duration / delay を `0ms` にする。\n- 非選択 swatch には黒い `box-shadow` / per-cell border を描かない。行全体が罫線化するのを避けるため、外周表現は親 strip の `::before` 背面レイヤーに限定する。\n- 選択 swatch の左右隣は、接する側に `var(--aegis-radius-medium)` の角丸を付ける。さらに 900 / 50 の外端も常時 `var(--aegis-radius-medium)` 角丸にし、swatch 単体の radius 変化をすべて 4px 相当に統一する。\n- Base の Foreground / Border 表示では、選択 tone の value swatch に色付き ring を表示する。関連 swatch は `--aegis-border-width-thinPlus`（1px）、クリックされた本体 swatch は `--aegis-border-width-thin`（2px）を使う。ただし border width 自体は transition せず、`.valueSwatch::after`（1px）と `.valueSwatch::before`（2px）の固定レイヤーを opacity で切り替える。これにより、tone 切り替え時の border-width animation によるカクつきを避ける。\n- Pane Start の App BG `TextField` は `PaletteLabContent` の `appBgLightnessInput` state を更新する。Aegis `TextField` は `type="number"`、`leading="L:"`、`min=0`、`max=100`、`step=0.01` を使う。blur 時に 0-100 へ clamp し、ContrastCheckPanel へ渡す前に `oklch(${lightness}% 0 0)` へ変換して Page BG / Surface BG token 解決の基底色として使う。\n- Pane Start の Background Combobox は palette 値選択時のみ `clearable` を有効にする。初期値の `None/default` では clear アイコンを表示しない。clear 操作時は `paneBackgroundRef` を `"default"` に戻し、`None` 背景へ復帰する。`PageLayoutPane` には `styles.palettePane` を付与し、`background-color` を `--aegis-motion-duration-normal` + `--aegis-motion-easing-default` で transition する。\n- Pane Start の contrast 表示トグルは Base / Primary / Transparent の全タブに適用する。Base は `SwatchRow` の `contrastRatios` で tone 色と選択中 Background の比率を表示する。Primary / Transparent は alpha 付き OKLCH を `cssColorToRgb(color, paneBackgroundRgb)` で選択中 Background に合成し、その実効 RGB と Background RGB の `wcagContrastRatio` を 1 桁小数で表示する。\n- Primary / Transparent の texture overlay は `paneBackgroundRef === "default"` のときだけ表示する。Combobox の Background が `None` の場合は従来の with texture 状態、palette 値が選ばれている場合は texture なしで選択背景に対する見え方を確認する。\n\n検証済み状態: lime-900 / lime-800 / lime-400 / lime-50 と amber-900 / amber-100 / amber-50 を選択した Base タブの computed style。strip は選択切り替え後も `left: 44` / `width: 344` / `height: 40` を維持し、実 background は transparent。strip `::before` は active row で `inset: -2px` / `backgroundColor: rgb(0, 0, 0)` / `borderRadius: 6px` / `transitionProperty: inset, background-color, border-radius` / `transitionDuration: 0.4s` / `transitionDelay: 0.08s` / `transitionTimingFunction: cubic-bezier(0.64, 0, 0, 1)`。選択 swatch は `borderRadius: 4px` / `transform: none`。通常選択の swatch `::before` は `top: 1px` / `bottom: 1px` / `left: 4px` / `right: 4px`、900 選択時は `left: 0` / `right: 4px`、50 選択時は `left: 4px` / `right: 0` になる。100 選択時は 50 swatch の外端と 200 swatch の選択接続側が `4px` radius になり、swatch 単体の radius が統一される。親 strip と非選択 swatch には `box-shadow` を付けない。\n\n---\n\n## 状態管理\n\n### アクション一覧 (store/actions.ts)\n\n| カテゴリ | アクション |\n|---------|----------|\n| プロジェクト | `ADD_PROJECT` / `DELETE_PROJECT` / `SELECT_PROJECT` / `RENAME_PROJECT` |\n| ファミリー | `ADD_FAMILY` / `DELETE_FAMILY` / `SELECT_FAMILY` / `RENAME_FAMILY` / `SET_PRIMARY_BASE_TONE` |\n| トーン | `ADD_TONE` / `DELETE_TONE` / `UPDATE_TONE` / `UPDATE_TONE_BULK` / `SELECT_TONE` |\n| インポート | `IMPORT_PALETTE` |\n\n### Reducer の重要な挙動\n\n- `UPDATE_TONE` payload に `lightness` / `chroma` / `hue` のいずれかが含まれると、`hex` を `oklchToHex` で自動再計算する\n- `UPDATE_TONE_BULK` はプロジェクト内の**全ファミリー**の同一 `toneValue` に適用（last write wins）\n- Neutral ファミリー (`isBuiltIn: true`) は `DELETE_FAMILY` で削除されない\n- `ADD_TONE` は `AEGIS_FIXED_LIGHTNESS` テーブルから初期 lightness を引く。テーブルにないトーンは L=50 にフォールバック\n\n### 永続化\n\n- キー: `palette-lab:v1` (localStorage)\n- `loadState()` / `saveState()` は `store/storage.ts`\n- state 全体をそのまま JSON でシリアライズ\n\n---\n\n## カラーユーティリティ\n\n### color/oklch.ts\n\n```typescript\n// OKLCH(L%, C, H) → #rrggbb (gamut クランプ済み)\noklchToHex(lightness: number, chroma: number, hue: number): string\n\n// #rrggbb → { l: number (0-100), c: number, h: number }\nhexToOklchChannels(hex: string): OklchChannels\n\n// ToneEntry → hex (shorthand)\ncomputeHex(tone: ToneEntry): string\n\n// 2値探索: sRGB 内に収まる最大 chroma (24 イテレーション)\nmaxSrgbChroma(lightness: number, hue: number): number\n\n// (L%, C, H) が sRGB gamut 内かどうか\nisInSrgbGamut(lightness: number, chroma: number, hue: number): boolean\n```\n\n- ライブラリ: [culori](https://culorijs.org/) (`clampGamut`, `formatHex`, `inGamut`, `oklch`, `parse`)\n- NaN hue (無彩色) は 0 に安全変換してから処理\n\n### color/primary.ts — Primary スケール生成\n\n```typescript\nbuildPrimaryScale(family: ColorFamily, alphaMap: Map<number, number>): PrimaryScaleEntry[]\n```\n\n- `alphaMap` は `buildNeutralAlphaMap(neutralFamily, origin?)` が返す `Map<toneValue, alpha>`（固定テーブル不使用）\n- `primaryBaseTone` (デフォルト `DEFAULT_BASE_TONE = 800`) の OKLCH チャンネルを基準色とする\n- **base tone**: `oklch(L% C H)` — 不透明\n- **base より明るいトーン**: `oklch(L% C H / alpha)` — `alphaMap.get(toneValue)` の alpha を使用\n- `alphaMap` に存在しないトーン → 除外（スケールに含めない）\n- base より暗いトーン → 除外（`value > baseToneValue`）\n- `ALT_TONE_SENTINEL` (alt500) は生成対象から除外\n- L/C/H は `resolveBaseChannels(baseTone)` が返す値（`AEGIS_FIXED_LIGHTNESS` 優先 + hex フォールバック）\n\n---\n\n## エクスポート\n\n### utils/export.ts — palette.json\n\n```typescript\nexportToPaletteJson(project): Record<string, Record<string, string>>\n// { "blue": { "50": "#f0f4ff", "alt500": "#3a6fd8", ... }, ... }\n```\n\n- `toneLabel()` でキー名を生成するため `alt500` が正しく出力される\n\n### utils/exportTokens.ts — palette.tokens.js\n\n出力フォーマット (DTCG 形式):\n\n```js\nexport default {\n  internal: {\n    color: {\n      palette: {\n        $type: "color",\n        scale: {\n          white: { 1000: { $value: "#ffffff", $deprecated: true } },\n          transparent: { $value: "transparent", $deprecated: true },\n          // 全ファミリーの生トーン (alt500 キーを含む)\n          blue: { "50": { $value: "#..." }, "alt500": { $value: "#..." }, ... },\n          neutral: { ... },\n          // 透過スケール (OKLCH native — Dynamic Alpha、origin 依存)\n          "white-transparent": { "900": { $value: "oklch(100% 0 0 / 0.94)" }, ... },\n          // neutral-transparent: appBgLightness >= 50 → oklch(0% 0 0 / alpha)、< 50 → oklch(100% 0 0 / alpha)\n          "neutral-transparent": { "900": { $value: "oklch(0% 0 0 / 0.94)" }, ... },\n        },\n        primary: {\n          // primaryBaseTone が設定された非 neutral ファミリーのみ\n          blue: {\n            "800": { $value: "oklch(29% 0.18 256.75)" },       // 不透明基準色\n            "700": { $value: "oklch(29% 0.18 256.75 / 0.73)" }, // 透過トーン\n            // ... 50 まで\n          },\n        },\n      },\n    },\n  },\n};\n```\n\n**Dynamic Alpha (固定テーブルなし)**:\n- alpha は `buildNeutralAlphaMap(neutralFamily, origin)` が neutral palette の実値から動的導出\n- `origin` は overlay base と alpha 算出式の両方を決める\n  - black-origin: `calcNeutralAlpha(hex) = round3(1 - R/255)` → `oklch(0% 0 0 / alpha)`\n  - white-origin: `calcWhiteAlpha(hex) = round3(R/255)` → `oklch(100% 0 0 / alpha)`\n- 固定の `TRANSPARENT_ALPHA_ENTRIES` / `PRIMARY_ALPHA_ENTRIES` 定数は削除済み\n\n---\n\n## コントラストチェック\n\n### 仕様定義 (color/contrast/specs.ts)\n\n- `ContrastPairDef`: `{ fg: string; bg: string; criterion: "text-normal" | "non-text"; state?: string; component?: string; id: string }`\n- コンポーネント 30+ 種の token ペアを宣言的に定義\n- WCAG AA: text-normal = 4.5:1、non-text = 3:1\n\n### トークン解決 (color/contrast/resolve.ts)\n\n`TOKEN_REFS` マップが semantic token 名 → パレット参照文字列（例: `"primary.blue.500"`）に変換する:\n\n#### `resolvePaletteRef` (composite RGB — contrast 計算用)\n\n| ref 形式 | 解決先 |\n|---------|-------|\n| `scale.white.1000` | `[255, 255, 255]` |\n| `scale.neutral.{tone}` | Neutral ファミリーの該当トーンの RGB |\n| `scale.neutral-transparent.{n}` | `compositeOver(origin==="white"?WHITE:BLACK, buildNeutralAlphaMap(fam,origin).get(n), over)` |\n| `scale.white-transparent.{n}` (inverse-transparent) | `compositeOver(origin==="white"?BLACK:WHITE, buildNeutralAlphaMap(fam,origin).get(n), over)` |\n| `primary.{name}.{tone}` | `cssColorToRgb(buildPrimaryScale(family, alphaMap).find(tone).oklch, over)` |\n| `scale.neutral.900` / `scale.neutral.700` / `scale.neutral.600` | brand-bold 系: neutral スケールに固定解決 |\n\n#### `resolveRefToCss` (CSS 文字列 — runtime token 用)\n\n| ref 形式 | 出力 |\n|---------|-------|\n| `scale.white.1000` | `"#ffffff"` |\n| `scale.neutral.{tone}` | Neutral HEX 文字列 |\n| `scale.neutral-transparent.{n}` | `"oklch(0% 0 0 / {alpha})"` (black-origin) / `"oklch(100% 0 0 / {alpha})"` (white-origin) |\n| `scale.white-transparent.{n}` (inverse-transparent) | `"oklch(100% 0 0 / {alpha})"` (black-origin) / `"oklch(0% 0 0 / {alpha})"` (white-origin) |\n| `primary.{name}.{tone}` | `buildPrimaryScale(...).find(tone).oklch` |\n\n**brand-bold トークンの参照**:\n```\n"brand-bold"         → scale.neutral.900\n"brand-bold-hovered" → scale.neutral.700\n"brand-bold-pressed" → scale.neutral.600\n```\nbrand-bold 系は neutral スケールに固定解決される（`brand.*` ファミリーには依存しない）。`Button variant="solid"` のデフォルト背景はパレットの brand 色ではなく neutral.900 が使われる。参照先は `scale.neutral.*` のため `isPaletteDependentRef` は `true` を返し、neutral palette の変更には追従する。brand family には依存しないが palette には依存する点に注意。\n\n**foreground.inverse トークンの参照**:\n```\n"inverse" → scale.neutral.50\n```\n`foreground.inverse` は固定 white (`scale.white.1000`) ではなく `scale.neutral.50` を参照する。`background.brand-bold = scale.neutral.900` の反対端として機能し、light project では neutral.50 = light (読める)、dark project では neutral.50 = dark (読める) と、neutral curve の変化に追従する。`scale.white.1000` 自体は固定 `#ffffff` のまま変更なし。\n\n**Button solid neutral の参照**:\n```\n"neutral-bold"         → scale.neutral.900\n"neutral-bold-hovered" → scale.neutral.700\n"neutral-bold-pressed" → scale.neutral.600\n```\n`Button` / `IconButton` の `solid·neutral` は brand 系ではなく neutral-bold 系を使う。`solid·neutral` の detail swatch が赤系で表示される場合は、描画ではなくチェック定義側の token mapping を疑う。\n\n### BgContext と computeResultsWithContext\n\nContrastCheckPanel が使う新 API。旧 `computePaletteContrastResults` の代替:\n\n```typescript\n// color/contrast/resolve.ts\nexport type BgContext = {\n  appBgRgb: RGB;   // App BG（Pane Start の Lightness number から oklch(L% 0 0) として解決）\n  pageBgRgb: RGB;  // Page BG（None / xSubtle / Subtle chip 選択から解決）\n  surfaceBgRgb: RGB; // Surface BG（None / Hovered / Pressed Select から解決）\n  neutralAlphaOrigin: NeutralAlphaOrigin; // appBgLightness < 50 → "white"、>= 50 → "black"\n};\n\nexport const computeResultsWithContext = (\n  pairs: ContrastPairDef[],\n  families: ColorFamily[],\n  ctx: BgContext,\n): PaletteContrastResult[] =>\n  pairs.map((pair) => {\n    // surfaceBgRgb を合成基底色として全トークンを解決\n    const overBg = ctx.surfaceBgRgb;\n    const bgRgb = resolveTokenRgb(pair.bg.category, pair.bg.key, families, overBg);\n    const fgRgb = resolveTokenRgb(pair.fg.category, pair.fg.key, families, bgRgb ?? overBg);\n    // ...ratio 計算・pass 判定...\n  });\n```\n\n### ContrastCheckPanel コンポーネント階層\n\n`components/ContrastCheckPanel/index.tsx`:\n\n**Latest checklist decisions**:\n- Text checks are treated as a foreground token audit. Normal surface checks include `accent.*` tokens for document/editor-style authored text colors, and exclude `*.pressed` tokens because those belong to component state behavior.\n- Ratio display uses `formatContrastRatio()` and renders two decimals for both pass and fail values. Fail values are floored to two decimals so a below-threshold value never appears as a passing threshold.\n- Summary settings include the `Ignore pressed states` checkbox. It defaults OFF; when ON, `state === "pressed"` checks are removed before `computeChecksWithContext()` and the settings icon shows an information Badge.\n\n```\nContrastCheckPanel                   # メイン export\n├── BgPreviewCard                    # ネスト矩形 BG 可視化 + トークン名ラベル\n├── PageBgChip (×3)                  # native <button type="button"> + 色見本 chip\n├── BG Settings div                  # Page BG / Surface BG の設定群。App BG は Pane Start 側から props で受け取る\n│   ├── BgSettingsRow + PageBgChip ×3 # Page BG chip selector\n│   └── BgSettingsRow + Select       # Surface BG (None/Hovered/Pressed/Selected)\n├── Summary bar div                  # Fail only / Normal|Inverse count / settings\n│   ├── Checkbox (Fail only)\n│   └── Count + Popover\n│       ├── Text (Normal|Inverse / {n}/{total})\n│       ├── IconButton size="xSmall" icon=LfSetting / information Badge when ignoring pressed\n│       └── Checkbox (Ignore pressed states)\n└── Slide viewport div\n    ├── List panel (absolute, translateX)\n    │   └── ComponentListView\n    │       └── Card variant="outline" > ActionList bordered size="large"\n    │           └── ActionList.Item (×n)            # fail Badge + chevron trailing\n    └── Detail panel (absolute, translateX)\n        └── ComponentDetailView\n            ├── Header (IconButton size="xSmall" Back + コンポーネント名)\n            └── StateSection (×states)              # state heading + result card\n                └── ResultRow (×pairs)              # preview | role + token名 + palette ref | icon | ratio\n```\n\n**Section label generation**:\n- `ComponentDetailView` は `[variant, state].filter(Boolean).join(" / ")` を section label に使う。\n- `fgSurface()` helper は state-less foreground-only checks 用なので `state` を付与しない。Text の `default` / `bold` / `subtle` や Link / Icon / FormControl label などは `variant` 名だけを表示し、`/ default` suffix を出さない。\n- Button / Input など、仕様上 `default` / `hovered` / `pressed` / `selected` を持つ component は各 check 定義で明示的に `state` を持つため、従来通り `variant / state` で表示される。\n\n**Text foreground token coverage**:\n- `TEXT_NORMAL_FOREGROUND_KEYS` は normal surface 上の Text foreground audit 対象を定義する。\n- 対象には `default` / `bold` / `subtle` / `xSubtle` / `brand` / `disabled`、status 系の `danger` / `information` / `success` / `warning` と各 `*-bold`、さらに document editor / Word 風の本文色変更で使われ得る `accent.*` foreground token を含める。\n- `pressed` / `danger-pressed` / `information-pressed` / `success-pressed` / `warning-pressed` は component 内 state に近い token なので Text の静的 foreground audit から除外する。Button / Link など state を持つ component 側の spec で必要に応じて扱う。\n- `inverse` は normal surface には混ぜず、既存通り `surfaceMode: "inverse"` の Text check として扱う。\n- `accent-gray-bold` は現状 `TOKEN_REFS.foreground` に存在しないため、spec では定義しない。\n\n**Page BG transition**:\n- `BG_COLOR_TRANSITION` は `background-color var(--aegis-motion-duration-normal) var(--aegis-motion-easing-default)`。\n- Page BG を切り替えると、Sidebar 全体、`BgPreviewCard` の Page / Surface 面、detail の `StateSection` card、`PageBgChip` の background-color が同じ motion token で補間される。\n- Page BG chip は background-color に加え、選択状態の `outline-color` も同じ duration/easing で transition する。\n\n**Collection/detail slide animation**:\n- list/detail の2パネルを同時に DOM に置き、`translateX(-100%) / translateX(0) / translateX(100%)` を切り替える。\n- `ResizeObserver` で active panel の高さだけを測り、slide viewport の `block-size` に反映する。非 active panel の高さは Sidebar のスクロール量に混ぜない。\n- non-active panel は `aria-hidden` と `pointerEvents: "none"` を付与する。\n\n**Detail Card layout**:\n- `ComponentDetailView` の outer `Card` は `padding: 0` / `gap: 0`。Aegis Card の内部 gap が Header と body の間に余白を作らないようにする。\n- Detail body 側で必要な余白だけを `--aegis-space-small` で管理する。\n\n**Fail only の適用範囲**:\n- `ComponentListView` は `failOnly` が ON の場合、`failCount > 0` の component だけを表示する。\n- `selectedResults` は detail 表示の入力点で、`failOnly` が ON の場合は選択中 component の `pass === false` 行だけに絞る。\n- `ComponentDetailView` は filtered results を state ごとに grouping するため、fail のない state section は detail 側にも表示されない。\n- ON にした結果、選択中 component に fail がない場合は detail 内に空状態テキストを表示する。\n\n**Ignore pressed states の適用範囲**:\n- Summary 右側の settings `IconButton size="xSmall"` から Popover を開き、`Ignore pressed states` Checkbox を操作する。\n- 初期値は OFF。既存の `COMPONENT_CHECKS` 件数と結果を維持し、pressed state も通常のコントラスト要件として扱う。\n- ON の場合、破壊的な許容操作として `COMPONENT_CHECKS` を `surfaceMode` で絞り込む段階で `state === "pressed"` の check を除外する。\n- Popover 内の補足文は “Pressed states will be excluded from contrast requirements.”。\n- ON 中は settings `IconButton` の icon を `Badge color="information"` で包み、通常チェック条件から外れていることを summary 上で示す。\n- 除外後の `filteredChecks` が `computeChecksWithContext()` に渡るため、summary count、component list の fail badge、detail の state section / result row はすべて同じ条件で再計算される。\n\n**palette ref 表示**:\n```typescript\nconst paletteRef = TOKEN_REFS[pair.fg.category]?.[pair.fg.key] ?? "—";\n// ResultRow は role → semantic token名 → palette ref の順で表示\n// 例: Foreground / foreground-inverse / scale.neutral.50\n```\n\n**Ratio display formatting**:\n- `formatContrastRatio(ratio, pass, threshold)` を通して表示する。\n- WCAG 判定は内部の生 ratio で行うため、たとえば `4.499` は `text-normal` の `4.5` を満たさず fail になる。\n- 表示は pass / fail とも `X.XX:1` の 2 桁に揃える。fail 値は丸めで合格値に見えないよう、2 桁で切り下げる。例: `4.499` は `4.49:1`、`2.999` は `2.99:1`。\n\n---\n\n## ToneCurve コンポーネント\n\n`components/ToneCurve/index.tsx` は SVG ベースの汎用グラフ:\n\n```typescript\ntype CurvePoint = {\n  toneValue: number;\n  channelValue: number;\n  color?: string;      // 制御点の色 (ToneAxisPanel でファミリー色に設定)\n  gamutMax?: number;   // Chroma カーブ用: sRGB 最大 chroma\n};\n\ntype ToneCurveProps = {\n  channel: "lightness" | "chroma" | "hue";\n  points: CurvePoint[];\n  onPointDrag: (toneValue, newChannelValue) => void;\n  columnHeader?: string;\n  width?: number;\n  height?: number;\n  gamutZones?: ReadonlyArray<readonly [number, number]>; // Hue 無効範囲\n};\n```\n\n**Gamut 可視化**:\n- `gamutMax` が設定された点: その値より上の領域をグレー塗りつぶし + 破線で境界表示 (Chroma 用)\n- `gamutZones`: `[lo, hi]` の無効 Hue 範囲を水平バンドでグレー表示 (Hue 用)\n- `FamilyAxisPanel` で `maxSrgbChroma` / `isInSrgbGamut` を使って計算し Props で渡す\n\n---\n\n## 本番コードとの対応\n\n| Sandbox | 本番相当 |\n|---------|---------|\n| `utils/export.ts` → `exportToPaletteJson()` | `palette.json` (パレット原本) |\n| `utils/exportTokens.ts` → `exportToPaletteTokensJs()` | `palette.tokens.js` (デザイントークン) |\n| `color/contrast/specs.ts` の TOKEN_REFS 定数 | `palette-config.json` の PRIMARY_SCALES / TRANSPARENT_SCALES |\n| `AEGIS_FIXED_LIGHTNESS` テーブル | Aegis パレット仕様書の Lightness 固定値 |\n| `ALT_TONE_SENTINEL = 450` | `palette.json` の `"alt500"` キー |\n\n---\n\n## 既知の制約・注意事項\n\n- **プロジェクト名の編集**: 現在は作成時のデフォルト名から変更できない (Open Question)\n- **Undo/Redo**: 実装なし。localStorage は上書きのみ\n- **マルチプロジェクト**: localStorage に全プロジェクトを保存するため、大量トーン・ファミリーでは容量に注意\n- **Hue shift なし**: 設計方針として全トーン統一 Hue。Hue shift が必要なケースはプロジェクト分割で対応\n- **Chroma スライダー上限**: `max=0.4` (OKLCH 仕様上の概算最大値。実際の gamut 上限はより低い場合がある)\n- **`ContentHeader.Title` / `.Description`**: Aegis で deprecated 済み。本番実装時は代替 API に変更する\n- **`<input type="range">`**: Aegis に Slider コンポーネントが存在しないため直接 HTML 要素を使用\n\n---\n\n## 依存ライブラリ\n\n| ライブラリ | 用途 |\n|-----------|-----|\n| `culori` | OKLCH ↔ sRGB 変換・gamut クランプ・gamut 判定 |\n| `@legalforce/aegis-react` | 全 UI コンポーネント |\n| `@legalforce/aegis-icons` | アイコン (`LfTrash` など) |\n';export{e as default};